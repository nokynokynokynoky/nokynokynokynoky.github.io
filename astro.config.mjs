// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
//export default defineConfig({});


export default defineConfig({
  site: 'https://nokynokynokynoky.github.io', // tu dominio principal
  output: 'static', // modo estático (no necesitas adaptador)
});